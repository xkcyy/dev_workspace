# AGENTS.md

本文件定义 `D:\01_DevWorkspace\05_Templates` 这一层的目录规则。

## 作用范围

- 适用于 `05_Templates` 及其未被更深层 `AGENTS.md` 覆盖的子目录。

## 当前层定位

- 这里用于可复用模板和片段。
- 本层只区分模板类型。

## 当前子目录

- `project-templates/`：项目模板
- `snippets/`：通用代码片段

## 简单规则

- 这里放可复用内容，不放具体业务项目。
- 更细规则由下层目录自行定义。
