# AGENTS.md

本文件定义 `D:\01_DevWorkspace\07_Docs` 这一层的目录规则。

## 作用范围

- 适用于 `07_Docs` 及其未被更深层 `AGENTS.md` 覆盖的子目录。

## 当前层定位

- 这里用于通用文档和知识沉淀。
- 本层只区分文档类型。

## 当前子目录

- `notes/`：笔记
- `architecture/`：架构文档
- `process/`：流程和规范

## 简单规则

- 放长期可复用的文档内容。
- 项目专属文档优先放在对应项目仓库中。
