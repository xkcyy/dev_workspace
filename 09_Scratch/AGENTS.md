# AGENTS.md

本文件定义 `D:\01_DevWorkspace\09_Scratch` 这一层的目录规则。

## 作用范围

- 适用于 `09_Scratch` 及其未被更深层 `AGENTS.md` 覆盖的子目录。

## 当前层定位

- 这里用于临时测试和一次性内容。
- 本层只区分临时内容类型。

## 当前子目录

- `snippets/`：可留存的小片段
- `tmp/`：临时文件

## 简单规则

- 这里的内容默认可以随时清理。
- 有长期价值的内容应迁移到正式目录。
