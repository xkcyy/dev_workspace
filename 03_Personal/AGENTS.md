# AGENTS.md

本文件定义 `D:\01_DevWorkspace\03_Personal` 这一层的目录规则。

## 作用范围

- 适用于 `03_Personal` 及其未被更深层 `AGENTS.md` 覆盖的子目录。

## 当前层定位

- 这里用于存放个人长期维护的项目。
- 本层只做项目类型分类。

## 当前子目录

- `apps/`：应用项目
- `tools/`：个人工具项目
- `libs/`：库和公共模块

## 简单规则

- 新项目优先放入已有类型目录。
- 项目内部规则由更深层 `AGENTS.md` 说明。
