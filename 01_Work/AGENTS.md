# AGENTS.md

本文件定义 `D:\01_DevWorkspace\01_Work` 这一层的目录规则。

## 作用范围

- 适用于 `01_Work` 及其未被更深层 `AGENTS.md` 覆盖的子目录。

## 当前层定位

- 这里用于存放公司或客户相关的工作内容。
- 本层只做工作内容分类，不描述具体项目实现细节。

## 当前子目录

- `lowcode/`：低代码相关项目
- `mom/`：MOM 相关项目
- `docs/`：工作文档
- `tools/`：工作辅助脚本和工具
- `temp/`：临时文件和短期内容

## 简单规则

- 工作相关内容优先放在本目录下。
- 具体项目规则由项目目录内的 `AGENTS.md` 继续说明。
